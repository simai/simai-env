# Optimization Plan

Use this item before applying server-wide optimization changes.

What it does:

- shows the current pressure picture,
- highlights the heaviest eligible pools,
- suggests a plan without applying it yet.

Choose it when:

- the server is oversubscribed,
- you need a read-only tuning proposal before action.
