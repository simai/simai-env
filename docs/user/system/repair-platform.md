# Repair Platform

Use this item when the base environment itself needs package or service repair.

Choose it when:

- required components are missing,
- bootstrap was incomplete,
- platform-level services need a managed repair pass.

Use it carefully:

- this is broader than checking one site,
- prefer `Platform status` first so you know why you are repairing.
