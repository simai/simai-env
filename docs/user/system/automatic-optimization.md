# Automatic Optimization

Use this item to turn the global automatic optimization policy on or off.

Choose it when:

- you want the platform to keep rebalancing eligible sites automatically,
- you need to suspend that automation during sensitive maintenance.

What to expect:

- this is global,
- per-site overrides still live in `Sites` Advanced mode.
