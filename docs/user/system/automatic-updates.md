# Automatic Updates

Use this item to inspect the current automatic update mode.

What it shows:

- whether automatic update behavior is off, check-only, or safe auto-apply,
- cached remote version state,
- last check time.

Choose it when:

- you need to know how aggressively the platform updates itself,
- you are deciding whether manual updates are still needed.
