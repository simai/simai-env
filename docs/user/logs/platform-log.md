# Platform Log

Use this item for the main operational log of `simai-env`.

Choose it when:

- a menu command failed and you need the server-side trace,
- platform automation or managed actions behave unexpectedly,
- support work points to the admin layer rather than the application.
