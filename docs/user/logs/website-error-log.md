# Website Error Log

Use this item as the main runtime error log for one site's nginx-facing failures.

Choose it when:

- pages return 500,
- upstream failures are suspected,
- PHP or routing problems appear during a request.

This is usually the most useful log right after `Diagnostics -> Site health check`.
