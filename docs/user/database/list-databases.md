# List Databases

Use this item for a read-only inventory of databases visible through managed root access.

Choose it when:

- you need to confirm whether a site's DB already exists,
- you are preparing maintenance,
- you need a quick DB-side overview without opening MySQL manually.
