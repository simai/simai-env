# WordPress Complete Setup

Use this item only after the browser installer has already finished.

What it does:

- applies the managed post-install baseline,
- aligns scheduler behavior,
- can enable HTTPS when offered,
- brings the WordPress site to an operational default state.

Do not use it as a substitute for the browser installer itself.
