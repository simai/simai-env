# Laravel Worker Status

Use this item when the site uses queue workers and you need to know whether they are healthy.

Choose it when:

- queued jobs are delayed or stuck,
- deployment changed queue-related code,
- you are verifying operational readiness after setup.
