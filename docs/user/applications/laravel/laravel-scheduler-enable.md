# Laravel Scheduler Enable

Use this item to install or restore the managed scheduler entry for `schedule:run`.

Choose it when:

- scheduled Laravel jobs are not running,
- the site has just been finalized,
- cron wiring was removed and must be restored.
