# Laravel Worker Restart

Use this item to restart managed Laravel queue workers.

Choose it when:

- deployment changed code used by workers,
- queue workers are stuck,
- status suggests the workers need a clean restart.
