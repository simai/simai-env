# Laravel Worker Logs

Use this item when queue jobs fail and status alone is not enough.

Choose it when:

- workers are crashing,
- jobs are retrying or failing,
- you need error details before touching queue configuration again.
