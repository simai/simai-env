# Laravel Cache Clear

Use this item when Laravel config, route, or view cache may be stale.

Choose it when:

- environment variables changed,
- routes or views behave like old code,
- you need a safe maintenance step before deeper debugging.
