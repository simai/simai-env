# Platform Status

Use this item to inspect the host itself rather than one application.

Choose it when:

- several sites are failing at once,
- DB, nginx, redis, or PHP-FPM may be unhealthy,
- you need to distinguish host problems from site problems.
