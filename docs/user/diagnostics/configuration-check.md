# Configuration Check

Use this item to detect managed config drift.

What it answers:

- does metadata still match generated config,
- is the site still aligned with the managed contract,
- is there known drift that `simai-env` can potentially repair.

Choose it when:

- someone edited managed files manually,
- runtime looks inconsistent,
- support work suggests configuration drift rather than application code errors.
