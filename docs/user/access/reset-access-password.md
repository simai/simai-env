# Reset Access Password

Use this item when the login stays the same but the password must change.

Choose it when:

- the password is lost,
- you are rotating credentials,
- access should continue without recreating the account.

What to expect:

- this changes credentials for the existing account,
- if the user works with SSH keys only, this may be unnecessary.
