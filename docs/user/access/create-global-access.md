# Create Global Access

Use this item to create an SFTP-only account that can see all projects under the managed root.

Choose it when:

- the operator really needs cross-project file access,
- project-scoped access would only create unnecessary friction.

Use it carefully:

- it is broader than project access,
- it is better suited for internal operators than for outside contractors.
