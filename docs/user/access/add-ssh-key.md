# Add SSH Key

Use this item to attach a public SSH key to an existing access account.

Choose it when:

- the account already exists,
- passwordless SFTP authentication is preferred,
- you want to avoid sharing or rotating plain passwords.

What to expect:

- you need the target login,
- you need a public key file path,
- this complements existing access; it does not create the account by itself.
