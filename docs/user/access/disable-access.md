# Disable Access

Use this item to temporarily block a managed access account without deleting it.

Choose it when:

- work is paused,
- the account should stop working immediately,
- you expect the same login to be re-enabled later.

Use this instead of `Remove access` when the block is temporary.
