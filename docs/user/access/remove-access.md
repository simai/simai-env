# Remove Access

Use this item to permanently delete a managed delegated access account.

Choose it when:

- the account should no longer exist,
- temporary disable is not enough,
- a project handoff is complete and the external login is no longer needed.

Best practice:

1. `Show access details`
2. decide whether `Disable access` is enough
3. only then `Remove access`
